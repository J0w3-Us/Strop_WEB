---
import Layout from "../../layouts/Layout.astro";
import Sidebar from "../../components/dashbord/Sidebar.astro";
import Topbar from "../../components/dashbord/Topbar.astro";
import "../../styles/dashbord.css";
---

<Layout title="Delete Account - Configuración">
  <div class="dashboard-layout">
    <Sidebar />

    <div class="main-content">
      <Topbar />

      <div class="content-area">
        <div class="proyectos-container perfil-container">
          <div class="proyectos-header">
            <h1 class="page-title">Delete Account</h1>
          </div>

          <div class="perfil-grid">
            <aside class="perfil-tabs">
              <nav>
                <a href="/configuracion" class="status-tab">Profile</a>
                <a href="/configuracion/teams" class="status-tab">Teams</a>
                <a href="/configuracion/reset-password" class="status-tab"
                  >Reset password</a
                >
                <a href="/configuracion/sign-as-user" class="status-tab"
                  >Sign As User</a
                >
                <a href="/configuracion/notification" class="status-tab"
                  >Notification</a
                >
                <a href="/configuracion/billing" class="status-tab">Billing</a>
                <a href="/configuracion/block-access" class="status-tab"
                  >Block Access</a
                >
                <a
                  href="/configuracion/delete-account"
                  class="status-tab active">Delete Account</a
                >
              </nav>
            </aside>

            <section class="perfil-main">
              <div class="sidebar-card">
                <h3 style="margin:0 0 16px;">Eliminar Cuenta</h3>
                <div
                  style="padding:20px; background:#fee2e2; border-radius:8px; border:1px solid #ef4444; margin-bottom:20px;"
                >
                  <div
                    style="font-weight:600; color:#991b1b; margin-bottom:8px;"
                  >
                    🚨 Acción Irreversible
                  </div>
                  <div
                    style="color:#991b1b; font-size:0.9rem; line-height:1.5;"
                  >
                    Esta acción eliminará permanentemente la cuenta y todos los
                    datos asociados. No podrás recuperar la información una vez
                    eliminada.
                  </div>
                </div>
                <div style="display:flex; flex-direction:column; gap:16px;">
                  <label style="display:flex; flex-direction:column; gap:6px;">
                    <span style="font-size:0.9rem; font-weight:500;"
                      >Para confirmar, escribe "ELIMINAR"</span
                    >
                    <input
                      type="text"
                      class="input-like"
                      style="padding:10px 12px;"
                      placeholder="ELIMINAR"
                    />
                  </label>
                  <label style="display:flex; flex-direction:column; gap:6px;">
                    <span style="font-size:0.9rem; font-weight:500;"
                      >Razón de la eliminación (opcional)</span
                    >
                    <textarea
                      class="input-like"
                      style="padding:10px 12px; min-height:100px; resize:vertical;"
                      placeholder="Ayúdanos a mejorar..."></textarea>
                  </label>
                  <button
                    class="btn-create-project"
                    style="align-self:flex-start; margin-top:8px; background:var(--color-danger); border-color:var(--color-danger);"
                  >
                    Eliminar Cuenta Permanentemente
                  </button>
                </div>
              </div>
            </section>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", function () {
      const currentPath = window.location.pathname;
      const navItems = document.querySelectorAll(".nav-item");
      navItems.forEach((item) => {
        item.classList.remove("active");
        const href = item.getAttribute("href");
        if (href && (currentPath === href || currentPath.startsWith(href)))
          item.classList.add("active");
      });
    });
  </script>
</Layout>
